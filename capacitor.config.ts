import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'p04-task-list',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
